name: Manual Hainan Airspace Fallback

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: hainan-airspace-manual-fallback
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Collect latest notices
        run: python scripts/update.py

      - name: Commit updated data
        run: |
          git config user.name "hainan-airspace-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No data changes"
          else
            git commit -m "chore: manual airspace report refresh"
            git push
          fi
